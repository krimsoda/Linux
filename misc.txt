utility
#download
wget [address] #download/saving
wget -O user_test1.sh https://raw.githubusercontent.com/krimsoda/Linux/main/sc_user_add.sh  #Saving Remote Files with a Specific File Name

curl [address] #contents display
curl -O [address] #download/saving
curl -o user_test1.sh https://raw.githubusercontent.com/krimsoda/Linux/main/sc_user_add.sh  #Saving Remote Files with a Specific File Name

git clone [git address]
git pull

History
set +o history # temporarily turn off history
# commands here won't be saved
set -o history # turn it back on
history -d [line] #delete a single command number 
