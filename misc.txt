utility
#download
wget
curl [address] #contents display
curl -O [address] #download/saving
curl -o do-bots.txt  https://www.digitalocean.com/robots.txt  #Saving Remote Files with a Specific File Name

git clone [git address]
git pull

History
set +o history # temporarily turn off history
# commands here won't be saved
set -o history # turn it back on
